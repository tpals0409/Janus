[project]
name = "janus-model-runtime"
version = "1.0.28"
description = "Janus MLX model runtime — dependency manifest for the local model server"
requires-python = ">=3.13"
license = "Apache-2.0"
authors = [{ name = "SeMin Kim" }]
dependencies = [
    "mlx-lm>=0.31.3",
    "mlx-vlm>=0.6.15",
    "openai>=3.3.1",
]

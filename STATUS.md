# Janus 상태 (커밋 c8c4c5a 이후)

로컬 우선 Agent Development Environment. Electron 앱 + FastAPI/LangGraph 서버 +
로컬 MLX 모델(Qwen3.8-27B 4bit).

**판정: 잘 만든 개념 증명 슬라이스. 완성품 아님.**
창 1개·실행 1개·백엔드 정상이면 전부 동작한다. "두 번째"가 등장하는 곳이 대부분 미처리이고,
일부 상태 표시가 자신 있게 거짓말한다.

---

## 다음에 할 일 (우선순위 순)

### 1. 안전 구멍 2개 — 수정 완료

**2026-08-22 수정 상태**

- C1: 위험 도구 승인을 `tools.dispatch` 계층으로 중앙화했다. 승인 콜백이 없거나
  거부하면 기본 거부하며, `agent`/`tool` 노드가 같은 게이트를 쓴다.
- C2: Electron이 기동별 256-bit 토큰과 허용 Origin을 서버/렌더러에 전달한다.
  HTTP는 `X-Janus-Token` + Origin, WebSocket은 서브프로토콜 토큰 + Origin을 모두 검증한다.
- 회귀 검증: 보안 테스트 8개, 도구/spec 자체 검사, Electron 프로덕션 빌드.

**수정 전 발견 내용**

**C1. `tool` 노드가 승인 없이 `run_bash`를 실행한다.**
승인 게이트가 `agent.py:189` 한 곳뿐이고, 위험 도구 검사(`spec.py:225`)는 `_check_agent`
(에이전트 노드 전용) 안에만 있다. `_make_tool`(`compile.py:155`)은 `dispatch`를 그냥 부른다.
→ 캔버스 `+ Tool` → `run_bash` → Run, 다이얼로그 없이 셸 실행.
**고치는 법**: 게이트를 노드 타입이 아니라 두 타입이 공유하는 dispatch 층으로 옮긴다.
데이터(`tools.DANGEROUS`, approver)는 이미 다 있다. 작은 수정.

**C2. CORS 와일드카드 + WS Origin 미검사 = 브라우저 경유 RCE.**
`server.py:50` `allow_origins=["*"]`, `server.py:268` `ws.accept()`에 Origin 검사 없음, 인증 0.
`set_workspace`(`server.py:196`)는 `/`도 받는다.
→ Janus 켠 채 아무 웹페이지나 열면 그 페이지가 tool 노드 그래프를 만들어 셸 실행(C1과 연쇄)
하거나 워크스페이스를 `/`로 옮겨 자격증명 유출.
**고치는 법**: origin 화이트리스트 + WS 핸드셰이크 검사 + 기동 시 생성한 토큰.

> `tools._resolve` jail 자체는 견고하다(`.resolve()` 후 `is_relative_to`). 문제는 jail을
> **옮길 수 있다는** 것.

### 2. 거짓 상태 (사용자를 속이는 것들)

- **H6 수정 완료:** 스팬이 있어도 `run_error`를 항상 표시하고, 실패 스팬이 하나라도 있으면
  실행 상태를 빨간 "● 실패"로 표시한다.
- **M7 수정 완료:** tool handler가 `{"error": ...}`를 반환하면 해당 tool 스팬을 `error`로
  마킹한다. trace 회귀 테스트로 보장한다.
- **H4 수정 완료:** Electron main이 janus-server/MLX를 2초마다 감독하고, 종료 시 지수
  백오프(최대 30초)로 재시작한다. 시작/재시작/반복 실패/외부 포트 점유를 IPC로
  렌더러에 전달해 무한 "로딩 중"으로 숨기지 않는다. 복구 시 미저장 편집도 보존한다.
- **M1 수정 완료:** 선택한 워크스페이스를 Electron `userData/state.json`에 0600 권한으로
  원자 저장하고 서버 기동 시 검증해 복원한다. 삭제된 경로는 기본값으로 폴백하며,
  저장 실패 시 메모리 경로도 롤백한다. 복원 경로가 달라지면 파일 트리/열린 파일을 무효화한다.

### 3. 핵심 루프 누락

**수정 완료:** A(현재/선택 실행)와 B(비교 실행)를 독립 상태로 보관해 동일 노드의
입력·출력·지연·토큰을 나란히 비교한다. 과거 실행을 B로 선택하거나, 현재 A를 B에
고정한 뒤 현재 입력으로 재실행할 수 있다. 히스토리 행에서도 해당 입력으로 즉시 재실행한다.

### 4. "두 번째" 미처리 (동시성)

- **H1** 동시 승인 요청 시 슬롯 하나(`store.ts:495`)가 덮어써짐 → 다른 워커 300초 매달림
- **H2** 실행 중 에이전트 전환 시 `openAgent`가 ws를 안 닫음 → 스팬이 엉뚱한 패널에 쌓임
- **H3** 더블클릭 Run → 이전 소켓 `onclose`(`store.ts:508`)가 새 실행의 ws를 null로
- **H5** 강제 종료 시 27B가 RAM에 고아로 남고, 다음 실행이 **옛 코드 백엔드에 조용히 붙음**
- **M5** 병렬 노드에서 토큰이 엉뚱한 노드에 귀속(`trace.py:104` 임의 open span)
- **M6** 순환 그래프가 트랜스크립트·토큰 이중 계산(span_start에서 sessions 리셋 안 함)

### 5. 나머지

미저장 편집이 에이전트 전환 시 확인 없이 소멸(M3) / 깨진 YAML 에이전트 클릭 시 500(M2) /
에이전트 삭제 시 runs 고아 → 같은 slug가 남의 히스토리 상속(M4, 이미 `runs/jail_test/` 발생) /
Stop이 llm 노드를 안 끊음(M10) / MLX 다운 시 Run이 안 막힘(M8)

**IDE 필수 부재**: Undo(Delete가 확인 없이 서브그래프 삭제), 키보드 단축키 전무,
출력 복사 버튼, 검색(에이전트 필터 disabled), 가상화 전무

**리포 수준 부재**: README, 정식 테스트(`demo()` 자체검사뿐), 배포 패키징(`pnpm dev` 전용),
코딩 에이전트 흡수 미완(도구만 이식, 그래프 없음)

---

## 잘 되어 있는 것 (건드리지 말 것)

- 이벤트 로그 단일 진실 원천 + `derive_messages` 파생
- 스트리밍 sink(에이전트 이벤트가 실행 중 실시간으로 나옴)
- blocking approver 왕복(워커 스레드 ↔ WS)
- 워크스페이스 jail(`_resolve`)
- 진짜 취소(생성 도중 스트림 닫음 — MLX 로그에서 29/500 토큰에 중단 확인)
- 프롬프트 트레이스(템플릿 치환 끝난 실제 전송 메시지)
- 정직하게 "아직 구현 안 됨" 표시된 것들(Inspector Test, 네비 스텁, Eval/Deploy, 읽기전용 배지)

## 점수

| 축 | |
|---|---|
| 데모/개념 증명 | ★★★★★ |
| 아키텍처 기초 | ★★★★☆ |
| 단일 사용자 해피 패스 | ★★★★☆ |
| 안전 | ★★☆☆☆ |
| 견고성(동시성·엣지) | ★★☆☆☆ |
| 상태 정직성 | ★★☆☆☆ |
| 제품 성숙도 | ★☆☆☆☆ |

## 실행법

```bash
cd janus && pnpm dev     # 앱이 janus-server(:8765)와 MLX(:8080)를 직접 띄우고, 끄면 함께 종료
```
백엔드 로그: `/tmp/janus-server.log`, `/tmp/janus-mlx.log`
